apiVersion: v1
kind: Pod
metadata:
  name: baseline-rejected
  namespace: security-demo
spec:
  containers:
  - name: privileged-container
    image: nginx:1.21
    securityContext:
      privileged: true
      runAsUser: 0
	
